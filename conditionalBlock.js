let myList = []
if (3 == 4) {
    myList.push("akarmi")
}
// myList is still empty

// if KEYWORD
// () - contains the condition (boolean)
// { } - contains 0 or more lines of code, it will run ONLY IF condition is true

if (true) { // condition can be anything that is a boolean (&&, ||, ==, !=, <, >=..., variables, values)
    
} else {
    // contains 0 or more lines of code, it will run ONLY IF condition is false
}